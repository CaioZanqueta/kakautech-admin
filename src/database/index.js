import Sequelize from "sequelize";
import config from "../config/database.js";
import User from "../models/user.js";import Project from "../models/project.js";import Task from "../models/task.js";import Client from "../models/client.js";import Ticket from "../models/ticket.js";import Comment from "../models/comment.js";import TimeLog from "../models/timelog.js";import ActivityLog from "../models/activitylog.js";import Holiday from "../models/holiday.js";import OvertimeRecord from "../models/overtimerecord.js";import Group from "../models/group.js";import OnCallSchedule from "../models/on-call-schedule.js";import TaskAssignee from "../models/taskassignee.js";import TaskComment from "../models/taskcomment.js";import TaskAttachment from "../models/taskattachment.js";import WorkLog from "../models/worklog.js";
const models=[User,Project,Task,Client,Ticket,Comment,TimeLog,ActivityLog,Holiday,OvertimeRecord,Group,TaskAssignee,TaskComment,TaskAttachment,WorkLog,OnCallSchedule];class Database{constructor(){this.connection=new Sequelize(config);this.init();this.associate()}init(){models.forEach(m=>m.init(this.connection))}associate(){models.forEach(m=>m.associate&&m.associate(this.connection.models))}}export default new Database();
