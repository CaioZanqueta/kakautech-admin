<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Meus Chamados - Portal do Cliente</title>
    <link rel="icon" type="image/webp" href="/public/favicon.webp" />
    <link rel="stylesheet" href="/public/portal.css" />
  </head>
  <body>
    <div class="app-layout">
      <%- include('../partials/portal-sidebar', { active: 'tickets' }) %>
      <%- include('../partials/portal-topbar', { breadcrumb: 'Portal / Meus Chamados' }) %>

      <main class="content">
        <div class="card">
          <div class="card-header-container">
            <div class="main-header">
              <h1>Meus Chamados</h1>
              <a href="/portal/tickets/new" class="btn-primary">Abrir Novo Chamado</a>
            </div>
            <div class="filters-and-search">
              <div class="filter-buttons">
                <a href="/portal/tickets?search=<%= currentSearch %>" class="btn-filter <%= currentStatus === '' ? 'active' : '' %>">Todos</a>
                <a href="/portal/tickets?status=open&search=<%= currentSearch %>" class="btn-filter <%= currentStatus === 'open' ? 'active' : '' %>">Abertos</a>
                <a href="/portal/tickets?status=in_progress&search=<%= currentSearch %>" class="btn-filter <%= currentStatus === 'in_progress' ? 'active' : '' %>">Em Andamento</a>
                <a href="/portal/tickets?status=closed&search=<%= currentSearch %>" class="btn-filter <%= currentStatus === 'closed' ? 'active' : '' %>">Fechados</a>
              </div>
              <form action="/portal/tickets" method="GET" class="search-form">
                <input type="hidden" name="status" value="<%= currentStatus %>">
                <input type="search" name="search" placeholder="Pesquisar por título..." value="<%= currentSearch %>" class="search-input">
                <button type="submit" class="btn-search" title="Pesquisar">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
                </button>
              </form>
            </div>
          </div>
          <% if (tickets.length === 0) { %>
          <p class="no-tickets">Nenhum chamado encontrado com os filtros atuais.</p>
          <% } else { %>
          <div style="overflow-x: auto;">
            <table>
              <thead>
                <tr>
                  <th>Título</th>
                  <th>Criado Por</th>
                  <th>Prioridade</th>
                  <th>Status</th>
                  <th>Última Atualização</th>
                </tr>
              </thead>
              <tbody>
                <% tickets.forEach(ticket => { %>
                <tr>
                  <td data-label="Título">
                    <a href="/portal/tickets/<%= ticket.id %>"><%= ticket.title %></a>
                  </td>
                  <td data-label="Criado Por"><%= ticket.Client ? ticket.Client.name : 'N/D' %></td>
                  <td data-label="Prioridade"><%= {low: 'Baixa', medium: 'Média', high: 'Alta'}[ticket.priority] || 'Média' %></td>
                  <td data-label="Status">
                    <span class="badge status-<%= ticket.status %>"><%= statusTranslations[ticket.status] || ticket.status %></span>
                  </td>
                  <td data-label="Última Atualização"><%= new Date(ticket.updatedAt).toLocaleDateString('pt-BR', { day: '2-digit', month: '2-digit', year: 'numeric'}) %></td>
                </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
          <% } %>
          <% if (totalPages > 1) { %>
          <div class="pagination">
            <a href="?page=<%= currentPage - 1 %>&status=<%= currentStatus %>&search=<%= currentSearch %>" class="<%= currentPage <= 1 ? 'disabled' : '' %>">Anterior</a>
            <span>Página <%= currentPage %> de <%= totalPages %></span>
            <a href="?page=<%= currentPage + 1 %>&status=<%= currentStatus %>&search=<%= currentSearch %>" class="<%= currentPage >= totalPages ? 'disabled' : '' %>">Próximo</a>
          </div>
          <% } %>
        </div>
      </main>
      <div class="mobile-overlay" id="mobile-overlay"></div>
    </div>
    <%- include('../partials/portal-scripts') %>
  </body>
</html>